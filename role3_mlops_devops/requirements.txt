# --- Web service ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
SQLAlchemy==2.0.36

# --- Monitoring ---
prometheus-client==0.21.1

# --- Model pipeline ---
# scikit-learn and xgboost are PINNED to the versions the artifacts were saved
# with. A mismatch unpickles with an InconsistentVersionWarning and can produce
# silently wrong results, so do not bump these without re-exported artifacts.
scikit-learn==1.9.0
xgboost==3.4.1
joblib==1.5.3
pandas==3.0.5
numpy==2.4.6
scipy==1.17.1

# FinBERT sentiment encoder.
transformers==5.15.0
# torch is intentionally NOT listed here. If it were, `pip install -r
# requirements.txt` would pull the multi-GB CUDA build from PyPI and overwrite
# a CPU install. Install it explicitly instead:
#   Docker : handled in the Dockerfile (CPU-only index)
#   Local  : pip install torch==2.13.0   (macOS gets the CPU/MPS build)

# --- Testing ---
pytest==8.3.4

# --- Postgres driver (used by the Docker Compose stack) ---
psycopg2-binary==2.9.10
