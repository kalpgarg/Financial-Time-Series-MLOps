# Prometheus configuration
# Scrapes the FastAPI /metrics endpoint for prediction latency, request count, etc.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "fints-api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
