# DVC pipeline definition
# Inference is now handled by role3 batch_score.py via the Airflow DAG.
# This file retains only the manual data-pull stage for local development.

stages:
  pull_data:
    cmd: dvc pull data/headlines.csv.dvc data/merged_ohlc_15min.csv.dvc
